import { createStitches } from '@stitches/react'

export const {
  config,
  styled,
  css,
  globalCss,
  keyframes,
  getCssText,
  theme,
  createTheme,
} = createStitches({
  theme: {
    colors: {
      background: ' #f6f6f6;',
      border: '#bdbdbd',
      grayfont: '#656565',
      title: '#0c0c24',
      green: '#54cbcb',
      blue: '#94e4dc',
    },
  },
})
